
// npm global command, comes with code
// npm--version

// local dependencies
// npm i package_name

// global dependencies
// npm install - g package_name
//sudo install -g package_name (macos)

// package.json -manifest file (stores important info about project/package)
// manual approach (create a package.jsoz in root, create properties etc )
// npm init (step by step press enter to skiip)
// mpm init -y (everything default)